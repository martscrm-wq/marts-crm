# 01 — Specify (المواصفة الوظيفية القابلة للتتبع)

> كل بند له معرّف فريد (`REQ-xxx`) يُستخدم كمرجع في `03-tasks.md`. هذا الملف "ماذا نبني" فقط — التفاصيل التقنية في `02-plan.md`.

## REQ-100 — Dashboard

| ID | الوصف | مصدر |
|---|---|---|
| REQ-101 | عرض 3 بطاقات: Hot (أحمر #FF4444) / Warm (برتقالي #FF8C00) / Cold (أزرق #2196F3)، كل بطاقة تعرض عدد الـ Leads الفعلي من IndexedDB | §2.1.1، يصلح B1 |
| REQ-102 | بطاقة Rotation اختيارية (رمادي)، تُخفى إن لم توجد بيانات دوران | §2.1.1 |
| REQ-103 | الضغط على أي بطاقة ينقل المستخدم إلى `/leads` مع فلتر `rating` مُفعّل مسبقًا مطابق للبطاقة | §2.1.2 |
| REQ-104 | الأعداد تُحدَّث فورًا (بدون إعادة تحميل الصفحة) عند أي تغيير في بيانات Leads عبر الاستماع لحدث `crm:lead:updated` | §2.1.2 |

## REQ-200 — Leads Management

| ID | الوصف |
|---|---|
| REQ-201 | نموذج إضافة Lead فردي بالحقول: name*, phone*, email, source*, rating*, stage*, assignedTo*, createdDate* (تاريخ/مدى), activityDate (تاريخ/مدى), assignmentDate (تاريخ/مدى), note, tags[] |
| REQ-202 | مكوّن "نطاق تاريخ" قابل لإعادة الاستخدام: زر تبديل بين "تاريخ واحد" و"من - إلى" |
| REQ-203 | عند حفظ Lead جديد يُولَّد `id` (Lead Code) تلقائيًا بصيغة `LD-000001` تسلسلي — **يصلح B14** |
| REQ-204 | الحقول التي بها اختيار مسبق تلقائي غير مقصود يجب أن تبدأ فارغة (placeholder) — **يصلح B5** |
| REQ-206 | تغيير مرحلة (Stage) الـ Lead **مباشرة من صف الجدول**: زر حالة داكن "Rotation ⏷" في كل صف يفتح قائمة منسدلة سريعة تُحدّث المرحلة فورًا — بجانب بقاء التغيير متاحًا من تفاصيل الـ Lead الكاملة |
| REQ-207 | تفاصيل الـ Lead تُعرض في **Slide-over Drawer** (بدل صفحة منفصلة): يُفتح بالنقر على اسم العميل، الخلفية تبقى ظاهرة معتّمة (Overlay شفاف)، اللوحة تنزلق من اليمين وتأخذ ~70% من العرض. مكوّناتها: (1) **عمود أيقونات داخلي داكن** `#1F3355` لقنوات التواصل (Avatar بحرفين ملوّن حسب المصدر + اتصال/محادثات/مرفقات بعداد/صور/كاميرا/جهات اتصال/موقع/نسخ + زر `···` يفتح قائمة Logs/Selling Units/Purchased Units)؛ (2) رأس: اسم العميل بخط كبير + أزرار إجراء دائرية ملوّنة دلاليًا + قائمة مرحلة "Rotation ▾" + `···` + إغلاق `✕`؛ (3) شريحة هاتف بحدود + وسوم ملوّنة؛ (4) **شبكة معلومات 4 أعمدة** (label رمادي صغير فوق value عريض، الحقول الفارغة تُعرض `--`، ASSIGNEES بصور دائرية متراكبة)؛ (5) تبويبا **Timeline** (نشط بخلفية زرقاء) / **Information** (حدود فقط) + زر "Show Lead Summary" + فلتر "All" منفصلان عن التبويبات؛ (6) **خط زمني**: دائرة أيقونة رمادية + بطاقة بحدود (نوع الحدث + حالته بخط عريض) + قائمة `···` لكل عنصر + رسالة نهاية رمادية |
| REQ-210 | زر استيراد جماعي بجانب زر `+`، يفتح صفحة `/leads/bulk-import` |
| REQ-211 | زر "تحميل نموذج Excel" ينزّل ملف `.csv` بالأعمدة الاثني عشر المحددة في §2.2.2 |
| REQ-212 | رفع ملف (csv/xlsx) → معاينة جدولية لأول التحقق من الأخطاء (صف بصف) قبل التأكيد النهائي |
| REQ-213 | صفوف غير صالحة (حقل مطلوب فارغ) تُعرض بلون أحمر مع سبب الخطأ ولا تُستورد |

## REQ-300 — Filters

| ID | الوصف |
|---|---|
| REQ-301 | بحث نصي فوري (debounce 300ms) بالاسم أو الهاتف |
| REQ-302 | فلاتر: نطاق تاريخ (لكل من created/activity/assignment)، stage، rating، source، assignedTo |
| REQ-303 | فلاتر متقدمة: تركيب شروط متعددة (AND) + فلتر جديد `Assign From (Agent X)` |
| REQ-304 | زر "تطبيق" على أي مجموعة فلاتر **يجب** أن يُحدّث الجدول فعليًا — **يصلح B4** (السبب الشائع: الفلتر لا يُربط بحدث submit أو المُحدِّد الخاطئ؛ تحقق من ربط `addEventListener` بالزر الصحيح ومن أن دالة `applyFilters()` تُستدعى فعليًا وتُعيد بيانات مفلترة من `store.js`) |
| REQ-305 | حفظ مخطط فلترة (اختياري، Phase 3) |
| REQ-306 | الفلاتر المُطبَّقة تُترجَم بصريًا إلى **Filter Chips** أعلى الجدول: كل فلتر نشط يظهر كـ Pill أزرق ممتلئ بصيغة `المفتاح: القيمة` + زر ✕ صغير لإزالته فرديًا، ورابط نصي **"Clear all"** لمسح كل الفلاتر دفعة واحدة، وزر **"Filter"** يعرض **عدادًا دائريًا أزرق** بعدد الفلاتر النشطة ويفتح لوحة الفلاتر المتقدمة — يربط REQ-303/REQ-304 بالواجهة المرئية (لا يُحدِّث الفلتر الجدول بصمت فقط) |

## REQ-400 — Bulk Actions

| ID | الوصف |
|---|---|
| REQ-401 | تحديد صفوف عبر checkbox + "تحديد الكل". أيقونات الإجراءات الجماعية (Assign, Tags, More, Edit, Settings, Add, Grid view) **ظاهرة دائمًا** في شريط الأدوات أعلى الجدول (وليست شريطًا عائمًا يظهر عند التحديد)، تبقى `disabled`/باهتة حتى يُحدَّد صف واحد على الأقل ثم تُفعَّل |
| REQ-402 | 13 عملية جماعية كما في §2.4.1: Reassign, Change Stage, Mark Todos Complete, Add Tags, Add Tasks, Bulk Nudge, Add to Campaign, Change Rating, Change Source, Change Wallet, Add Bulk Note, Merge Leads, Bulk Delete |
| REQ-403 | كل عملية جماعية **يجب أن تنفّذ فعليًا** تعديلًا حقيقيًا على `store.js` وتُحدّث الجدول — **يصلح B3** (تحقق أن الزر مربوط بمعالج، وأن المعالج يستدعي `store.bulkUpdate(ids, patch)` وليس دالة وهمية فارغة) |
| REQ-404 | Bulk Delete يتطلب نافذة تأكيد ثانية تعرض العدد المتأثر بوضوح |
| REQ-405 | Bulk Nudge يُنشئ عنصر Task/Follow-up مرئي لكل Lead محدد (وليس مجرد رسالة toast) — **يصلح B6** |
| REQ-410 | سجل تاريخ العمليات الجماعية: جدول بالأعمدة (الوقت، المُنفِّذ، النوع، العدد المتأثر، الحالة، زر تراجع) |
| REQ-411 | زر "تراجع" يُعيد كل الحقول المتأثرة إلى قيمتها قبل العملية (يتطلب حفظ snapshot لكل عملية) — **يصلح B13** |

## REQ-500 — Deals

| ID | الوصف |
|---|---|
| REQ-501 | جدول Deals، كل صف مرتبط بـ `leadId` |
| REQ-502 | الضغط على اسم العميل داخل صف الصفقة يفتح **Drawer تفاصيل الـ Lead** (REQ-207) — **يصلح B8** (تحقق أن اسم العميل عنصر قابل للنقر `<a>`/button مع `leadId` صحيح وليس نصًا ثابتًا) |
| REQ-503 | صفحة Insights: رسم بياني بسيط (canvas/SVG يدوي بدون مكتبة خارجية إلزامية، أو Chart.js عبر CDN إذا سُمح) يعرض: عدد الصفقات حسب الحالة، القيمة الإجمالية حسب الشهر |
| REQ-504 | فلاتر خاصة بالصفقات: تاريخ، حالة،代agent |
| REQ-505 | زر "Close" ظاهر على كل صفقة مفتوحة، يفتح قائمة اختيار `Closed Won` / `Closed Lost` — **يصلح B12 (كان غائبًا)** |
| REQ-506 | عملية جماعية: إغلاق صفقات قديمة دفعة واحدة بنفس منطق REQ-505 |
| REQ-507 | أيقونة الإعدادات ⚙️ في صفحة Deals تفتح لوحة/صفحة إعدادات فعلية — **يصلح B9** |

## REQ-600 — Marketing

| ID | الوصف |
|---|---|
| REQ-601 | قائمة الحملات: بحث + فلاتر + زر `+ New Campaign` |
| REQ-602 | نموذج حملة: name, type, startDate, endDate, targetAudience, status (Draft/Active/Completed) |

## REQ-700 — Inventory

| ID | الوصف |
|---|---|
| REQ-701 | قائمة الوحدات مع بحث وفلاتر |
| REQ-702 | البحث يشمل: العنوان، الموقع، نوع الوحدة، السعر — وليس الهاتف فقط — **يصلح B11** |
| REQ-703 | كل أيقونات الإجراءات (تعديل/حذف/عرض) على كل وحدة يجب أن تكون مربوطة فعليًا بمعالج حدث — **يصلح B10** |
| REQ-704 | صفحة تفاصيل الوحدة: View / Edit / Update / Delete |
| REQ-705 | تحديد وقت النشر (Publish Time) بعد إكمال كل تفاصيل الوحدة الإلزامية فقط |
| REQ-706 | توليد رابط نشر عام (`publishUrl`) عشوائي فريد يُخزَّن على الوحدة |
| REQ-707 | صفحة عامة منفصلة (`/public/unit.html?code=...`) تعرض تفاصيل الوحدة وتحتوي نموذج عميل: name*, phone*, email, note |
| REQ-708 | إرسال هذا النموذج العام ينشئ Lead جديد تلقائيًا في المخزن (source = "Public Listing") |
| REQ-709 | نموذج إضافة وحدة: نوع، مساحة، سعر، موقع، تخطيط، وصف، صور (تخزين كـ base64/blob في IndexedDB) |

## REQ-800 — Exports

| ID | الوصف |
|---|---|
| REQ-801 | تصدير CSV: نص مفصول بفواصل مطابق فعليًا لأعمدة الجدول الحالي المعروض (بعد الفلترة) |
| REQ-802 | تصدير Excel: ملف `.xlsx` حقيقي عبر SheetJS، وليس CSV بامتداد مختلف — **يصلح B7** |
| REQ-803 | تصدير PDF: جدول منسّق فعليًا (عبر jsPDF-AutoTable أو رسم يدوي)، ليس نفس محتوى CSV |
| REQ-804 | تصدير JSON: مصفوفة كائنات JS مطابقة لنموذج البيانات في `02-plan.md`، بمسافات بادئة (pretty-print) |
| REQ-805 | كل صيغة تصدير تُنتج ملفًا مختلفًا فعليًا يمكن التحقق منه بفتح الملفين ومقارنتهما — **معيار قبول B7** |

## REQ-900 — UX عامة

| ID | الوصف |
|---|---|
| REQ-901 | Toast notification لكل عملية ناجحة/فاشلة (أعلى يمين، تختفي تلقائيًا بعد 3 ثوانٍ) |
| REQ-902 | مؤشر تحميل (spinner/skeleton) أثناء أي عملية IndexedDB > 200ms |
| REQ-903 | شريط تقدم للعمليات الجماعية على أكثر من 20 عنصرًا |
| REQ-904 | تصميم متجاوب: desktop أولوية، ثم tablet، ثم أساسيات mobile (breakpoints: 1024px, 768px) |

## مصفوفة تتبع الأخطاء (Bug Traceability Matrix)

| Bug | REQ المرتبط | الأولوية |
|---|---|---|
| B1 | REQ-101 | High |
| B2 | REQ-703, REQ-507 (وأي أيقونة أخرى تُكتشف أثناء المراجعة اليدوية) | Medium |
| B3 | REQ-403 | High |
| B4 | REQ-304 | High |
| B5 | REQ-204 | Medium |
| B6 | REQ-405 | Medium |
| B7 | REQ-802, REQ-803, REQ-804, REQ-805 | Medium |
| B8 | REQ-502 | High |
| B9 | REQ-507 | Medium |
| B10 | REQ-703 | Medium |
| B11 | REQ-702 | Medium |
| B12 | REQ-505, REQ-506 | High |
| B13 | REQ-410, REQ-411 | Medium |
| B14 | REQ-203 | High |
