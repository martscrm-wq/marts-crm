{
  "name": "marts-crm",
  "version": "2.5.0",
  "private": true,
  "description": "Marts CRM/ERP System",
  "scripts": {
    "test": "jest --coverage",
    "test:unit": "jest --selectProjects unit",
    "test:ci": "jest --ci --coverage --coverageReporters=json-summary --coverageReporters=text"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
