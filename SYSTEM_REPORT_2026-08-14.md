# التقرير الشامل لنظام Marts CRM/ERP
**الإصدار:** 2.11.0 | **تاريخ التقرير:** 14 أغسطس 2026 | **الحالة:** منشور حيًا (موقعان)

---

## 1. نظرة عامة
نظام **Marts CRM/ERP** — نظام متكامل لإدارة العقارات (مبيعات + CRM + موارد بشرية + رواتب + محاسبة + حوكمة + تسويق + تقارير)، مبني كتطبيق ويب أحادي الملف (Single-File App) يعمل بالكامل في المتصفح مع مزامنة سحابية اختيارية.

| البند | القيمة |
|---|---|
| اسم الملف | `Marts_System_Merged.html` (نسخ مطابقة ×3) |
| الحجم | 2.36 MB |
| عدد الأسطر | 34,771 |
| عدد الدوال | 1,205 |
| النسخة الحالية | 2.11.0 (`SYSTEM_VERSION` + `version.json`) |
| واجهة اللغة | عربي (RTL) / إنجليزي |
| التوافق | جوال/لوحي/سطح مكتب + PWA (sw.js) |

## 2. المعمارية والتقنيات
- **البنية:** ملف HTML واحد يضم CSS + JS + واجهات (لا خادم تطبيق؛ مزامنة سحابية عبر REST)
- **التخزين المحلي:** `localStorage` عبر طبقة `STORAGE` (1019 قراءة / 312 كتابة في الكود) — لا وصول مباشر لـ localStorage في الكود (0)
- **السحابة:** Firebase — مشروع `marts-crm-v2` (المصدر الرسمي للبيانات الحية):
  - المجموعات: `app_data`, `activity_logs`, `app_data_backup`, `system_config`
  - المشروع القديم `marts-crm-6ca37` متوقف (استنفد الحصة)
- **النشر:** Vercel — موقعان:
  1. `https://marts-crm.vercel.app` (من مجلد `public`)
  2. `https://namecheapdeploy.vercel.app` (من مجلد `namecheap_deploy`)
- **مكتبات مضمّنة:** SheetJS (XLSX) للتصدير، PWA (Service Worker) للتحديثات والاستخدام دون اتصال
- **النسخ الاحتياطي:** مهمة مجدولة `MartsCRM_CloudBackup` يوميًا 01:00 → `backups\cloud-backup-*.json` (يحتفظ بآخر 14 نسخة)

## 3. الوحدات الوظيفية
### الصفحات الرئيسية (7)
`dashboard` (لوحة القيادة) · `sales` (المبيعات) · `crm` (إدارة العملاء) · `marketing` (التسويق) · `reports` (التقارير) · `governance` (الحوكمة) · `myAccount` (حسابي)

### المحاسبة — 18 تبويبًا
unified · treasury · bank · salaries · partners · expenses · coa · journal · franchises · financials · analysis · cashFlow · expenseAnalysis · revenueAnalysis · risk · **fixedassets** (جديد 2.11.0) · transactions · intercompany

### أبرز الوحدات الوظيفية (~160 دالة عرض)
| الوحدة | أبرز المكونات |
|---|---|
| **المبيعات** | عمليات بيع، حجوزات، تعاقدات، تحصيلات، عمولات (قواعد/شهرية/توزيعات)، فرق إعادة بيع، ضريبة 14% |
| **CRM** | ملف عميل شامل (12 تبويب)، خط أنابيب، صفقات، أحداث، جرد، حملات، فرق، إعدادات متقدمة، مصدر العميل، ملاحظات المندوبين |
| **الموارد البشرية** | موظفون، هيكل تنظيمي، شجرة فروع، توظيف/طلبات وظائف (نموذج شامل + QR)، مقابلات، تقييمات |
| **الرواتب** | كشوف رواتب تفصيلية (حضور/غياب/خصومات/حوافز KPI)، سلف، إجازات، غياب استثنائي، **تأمينات + ضريبة دخل** (2.11.0) |
| **الانضباط** | جزاءات مع تاريخ خصومات مفصل (Entitled/Non-Entitled)، لائحة داخلية قابلة للتعديل |
| **المحاسبة** | شجرة حسابات (COA)، يومية، ميزان مراجعة، قيود تلقائية مترابطة (sourceId)، تسويات بنكية + استيراد CSV، تدفقات نقدية، تحليل مالي/عمودي، شركات intercompany، **أصول ثابتة وإهلاك** (2.11.0) |
| **الحوكمة** | نظرة عامة، سياسات، تفويض صلاحيات (DOA)، شكاوى، فحص صلاحيات، تدقيق فصل مهام (SOD)، سجل أنشطة |
| **التقارير** | ميزانية عمومية، قائمة دخل، تدفق نقدي، تحليل إيرادات/مصاريف، تقارير CRM |
| **الإشعارات** | إشعارات داخلية لكل الأقسام + **إشعارات تلقائية** (انتهاء عقود/تراخيص/فترة اختبار — 2.11.0) |
| **الإعدادات** | مستخدمون وصلاحيات (14+ دورًا)، نسخ احتياطي، استيراد/تصدير، سجل تحديثات، تكاملات |

## 4. الأمان
| الإجراء | الحالة |
|---|---|
| نظام صلاحيات `hasPermission` | ✅ 70 استدعاءً عبر 20+ صلاحية محاسبية ووحدات أخرى |
| فصل المهام (SoD) | ✅ المصادق لا يمكنه اعتماد سلفة/كشف رواتب أنشأه بنفسه |
| استثناء مراجع للقيود التلقائية | ✅ صلاحية `accounts_journal_exception` + أثر تدقيق `journal_exception_edit` |
| حماية القيود التلقائية | ✅ `autoGenerated/sourceId/locked` — غير قابلة للتعديل/الحذف العادي |
| ترميز المخرجات (XSS) | ✅ دالة `escapeHtml` (8 استخدامات) — طبقت على وحدات الأصول والمستندات (2.11.0) |
| تسجيل التدقيق | ✅ `govAuditLog` (14 موقعًا) — إجراءات الأصول الثابتة والإهلاك والاستثناءات |
| بيانات الاعتماد | ⚠️ 29 قيمة `password:` مضمّنة (حسابات افتراضية/وضع فحص) — ذات خطورة منخفضة لكن يُوصى بمراجعتها |
| `eval(` | 1 موضع في محرك قوالب قديم (L34019) — غير مستخدم في الدوال الجديدة |
| `document.write` | 2 موضع (طباعة عقود/نماذج في نوافذ جديدة) |
| تخزين Base64 | ✅ حد حجم 1.5MB لرفع مستندات الموظفين (حماية localStorage) |

## 5. الأداء
| المقياس | القيمة |
|---|---|
| `innerHTML` (عرض ديناميكي) | 426 — النمط السائد؛ الوحدات الجديدة تستخدم نفس النمط بتكوين محلي |
| `confirm()` / `alert()` | 90 / 206 — تحويل تدريجي إلى `wsToast` (256 استدعاء) |
| تحميل الصفحة | ملف واحد 2.36MB مع PWA (تخزين مؤقت) — لا طلبات شبكة إضافية للتشغيل الأساسي |

## 6. الاختبارات
| المجموعة | العدد | الحالة |
|---|---|---|
| CRM unit tests (`C:\Users\khelw\Downloads\CRM`, Jest) | **114** (89 + 25 جديدة) | ✅ ناجحة |
| CI/CD pipeline (`C:\cicd-pipeline`) | **56** | ✅ ناجحة |

تغطي: أدوات مساعدة، تخزين، أدوار وصلاحيات، مستخدمون/موظفون، قيود تلقائية، سجل أنشطة + **تأمينات/ضريبة/إجازات/إهلاك/escapeHtml** (جديدة)

## 7. سجل الإصدارات (آخر 10)
| الالتزام | التاريخ | الوصف |
|---|---|---|
| `942e449` | 2026-08-14 | تحصين أمني 2.11.0 (XSS + صلاحيات + حد حجم) + 25 اختبارًا جديدًا |
| `99ac0a6` | 2026-08-14 | رفع الإصدار إلى **2.11.0** (اكتمال 14 بند تحسين) |
| `6e91cba` | 2026-08-14 | المرحلة 3: أصول ثابتة وإهلاك + أرشيف مستندات الموظفين |
| `1bc213c` | 2026-08-14 | المرحلة 2أ: نماذج ضريبية 4/6 + استثناء القيود + إشعارات + SoD |
| `18e4bae` | 2026-08-14 | المرحلة 1: تأمينات (148/2019) + ضريبة دخل + إجازات تلقائية + خصم السلف |
| `95811d8` | 2026-08-14 | استعادة ميزة Feedback CRM بعد استرجاع جزاءات |
| `3cc9cc9` | 2026-08-13 | تاريخ خصومات الموظف (Entitled/Non-Entitled) |
| `b7c0fbc` | 2026-08-13 | سجل عميل CRM: اسم المندوب + ملاحظة |
| `eca6c6f` | 2026-08-13 | تعديل/حذف/إنشاء اللائحة الداخلية |
| `b3dabd5` | 2026-08-13 | مزامنة الإصدار 2.10.1 + تحديث sw.js |

المسار الكامل: 40+ التزامًا منذ 2026-08-05 (تحليل مالي، استثناءات حضور، تطبيقات وظائف، قيود تلقائية، CRM v2، ترحيل سحابي marts-crm-v2...)

## 8. البيانات والتخزين
- مفاتيح رئيسية: `employees` (190 موضع) · `sales` (185) · `treasury` (55) · `chartOfAccounts` (48) · `employeeDeductionHistory` (45) · `journalEntries` (40) · `employeeAdvances` (16) · `crm_clients` (16) · `notifications` (16) · `fixedAssets` (14) · `employeeDocs` (11) · `activityLogs` (11) · `bankStatements` (4)
- قوائم المفاتيح محصّنة في 5 سجلات (keys, critical, COMPANY_STORAGE_KEYS, allKeys, backupKeys)
- حماية ضد الكتابة الفوقية الفارغة (empty-overwrite block) + حذف كامل بصلاحية owner + تأكيد + نسخة سحابية

## 9. الحالة الحية
- ✅ `marts-crm.vercel.app` — HTTP 200 (2,374,465 حرف)
- ✅ `namecheapdeploy.vercel.app` — HTTP 200 (2,374,465 حرف)
- ✅ `version.json` = 2.11.0

## 10. التوصيات المستقبلية
1. مراجعة القيم المضمّنة لبيانات الاعتماد (29 موضعًا) ونقل الحسابات الافتراضية إلى إعداد مركزي
2. تحويل `confirm/alert` المتبقيين إلى `wsToast`/نوافذ مخصصة
3. رفع حد المساحة (سعة localStorage) بضغط بيانات المستندات (ضغط Base64) أو نقلها لسحابة
4. تغطية اختبارية إضافية لوحدات الحوكمة والـ CRM
5. توثيق REST/سحابة وأتمتة نشر CI من git (حاليًا يدوي عبر `vercel --prod`)